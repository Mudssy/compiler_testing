
#include <stdio.h>
#include <math.h>

int main() {
    double x = M_PI / 3; // define x as PI/3
    double y = cos(x);  // calculate the cosine of x
    
    printf("cos(pi/3) is %f\n", y); // print the result
    
    return 0;
}
