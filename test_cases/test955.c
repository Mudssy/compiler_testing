
#include <stdio.h>

int main() {
    printf("LLVM Bitcode Reader and Writer Test: ");
    
    int a = 10;
    int b = 20;
    int result = a + b;

    printf("Result of addition (%d + %d) is: %d\n", a, b, result);

    return 0;
}
