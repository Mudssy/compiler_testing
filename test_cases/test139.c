
#include <stdio.h>

int main() {
  int result = 0;
  
  // Declare a variable and perform some operation on it to identify symbols
  int a = 10;
  result += a * 2;

  printf("Result: %d\n", result);
  return 0;
}
