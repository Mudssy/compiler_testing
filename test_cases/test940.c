
#include <assert.h>
#include <stdio.h>

int main() {
    int x = 10;
    printf("x: %d\n", x);
    
    // The CHECK directive will verify if "x: 10" is in the output of this program.
    return 0;
}
