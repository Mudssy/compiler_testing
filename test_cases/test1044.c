
#include <stdio.h>

int main() {
    int a = 2;
    int b = 3;
    
    printf("No spaces: %d\n", a+b);      // no spaces around operator '+'
    printf("Spaces: %d\n", a + b);      // spaces around operator '+'
    return 0;
}
