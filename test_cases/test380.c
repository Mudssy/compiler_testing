
#include <stdio.h>

int main() {
    int x = 5; // Declare an integer variable
    float y = (float)x / 2; // Cast x to float and then divide by 2
    printf("%f\n", y); // Print the result
    return 0;
}
