
#include <stdio.h>

int main() {
    int result = 0;
    if(result == (1 + 2)) {
        printf("Result is: %d\n", result);
    } else {
        printf("No spaces inside brackets feature.\n");
    }
    return 0;
}
