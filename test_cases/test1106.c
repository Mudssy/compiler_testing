
#include <stdio.h>

int main() {
    int x = 10;
    if (x > 5) {
        int y = 20;
        printf("x is greater than 5\n");
        printf("y is %d\n", y);
    } else {
        int z = 30;
        printf("x is not greater than 5\n");
        printf("z is %d\n", z);
    }
    return 0;
}
