
#include <stdio.h>
#include <math.h>

int main() {
    double num = 3.14; // You can replace this with any number you want to test
    printf("Ceil of %lf is %lf\n", num, ceil(num));
    return 0;
}
