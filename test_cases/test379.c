
#include <stdio.h>
int main() {
    int a = 10;
    int b = 20;
    // Using ternary operator to find the maximum of two numbers
    int max = (a > b) ? a : b;
    printf("The maximum value is: %d\n", max);
    return 0;
}
