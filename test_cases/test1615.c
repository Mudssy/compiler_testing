
#include <stdio.h>

int main(void) {
    // This complex expression should be simplified for the purpose of testing
    int result = (1 + 2 * 3 - 4 / 5 % 6);
    
    printf("The result is: %d\n", result);
    
    return 0;
}
