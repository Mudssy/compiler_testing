
#include <stdio.h>
#include <math.h>

int main() {
    double x = 2.0; // input value
    double result = cosh(x);
    
    printf("The hyperbolic cosine of %lf is: %lf", x, result);

    return 0;
}
